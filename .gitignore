.claude/
.gstack/
.idea/

# Crash-дампы Git Bash на Windows — мусор, не трекаем
*.stackdump

# Локальные .env с секретами — НЕ коммитим; трекаются только образцы *.env.example
.env
.env.*
!.env.example
AGENTS.md
# Velobike API capture (contains session token/PII — never commit)
*.har
