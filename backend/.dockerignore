# Build/local artifacts — kept out of the build context (rebuilt in the image).
build/
.gradle/
# Local dev photo-drop frames (danchuo.film.storage-dir default) — never part of the image.
data/
.idea/
*.iml
*.log
