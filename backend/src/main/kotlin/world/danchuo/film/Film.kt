/**
 * Feature-слайс **film** (PRD §5.12, §7, §12 M4; DESIGN §7.5) — каркас фото-дропов.
 *
 * Фото выкладываются дропами (≈36 кадров за день, как альбом-событие). Модель и экраны
 * специфицированы; **канал загрузки кадров — B1-админка** (PRD §9 п.7): ~36 файлов нельзя
 * залить iOS-шорткатом. До B1 эндпоинты отдают пусто (`GET /api/drops` → `[]`,
 * `GET /api/drops/{id}` → 404), фронт-тайл и модалка живут в пустом состоянии — борд не ломается.
 *
 * `FilmDrop`/`FilmPhoto` (`width/height` нужны клиенту для justified-композиции, §5.12) →
 * репозитории → `FilmDropView`/`FilmPhotoView` → [FilmResource]. Это специфицированный шов
 * (не спекулятивный): сущности на месте, данные придут в B1 без правок ядра (§3.1 guardrail).
 */
package world.danchuo.film
