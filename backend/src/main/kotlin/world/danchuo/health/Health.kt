/**
 * Feature-слайс **health** (PRD §3.1) — пустой шов M0.
 *
 * Зона ответственности (M1): `Workout` и сырые показатели здоровья (шаги/сон/тренировки),
 * идемпотентный `ingest/health` (push с iOS-шорткатов, upsert по дате за bearer).
 * Сон относится ко дню пробуждения; агрегаты — в каноне MSK.
 *
 * Заметка: экранное время Apple программно недоступно — сущность/эндпоинт не заводим
 * (PRD §9 B2, guardrail §3.1); ручное поле `screenTimeMinutes` живёт в `days`.
 */
package world.danchuo.health
