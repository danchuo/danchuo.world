/**
 * Feature-слайс **monster** (PRD §3.1, §5.6; DESIGN §6) — пустой шов M0.
 *
 * Зона ответственности (M1): `MonsterFlavor` — вкусы энергетика (data-driven:
 * новый вкус = запись в БД), у каждого `accentColor` для пиксель-метки дня.
 * «Монстр дня» = выбранный вкус либо «не пил»; день в цвет вкуса не красится.
 */
package world.danchuo.monster
