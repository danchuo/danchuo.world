/**
 * The **projects** slice: `Project` (data-driven) through its repository to `GET /api/projects`.
 * Filled by seed or API, so a new project is a row and not a release; schema and seed live in
 * `db/changelog/changes/0060-project.xml`. PRD §3.1, §5.7
 */
package world.danchuo.projects
