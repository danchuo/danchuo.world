/**
 * The **social** slice: `SocialLink` and `Artifact` (marquee items) through their repositories to
 * `GET /api/social-links` and `GET /api/artifacts`. Data-driven — a new link or artifact is a row;
 * schema and seed live in `db/changelog/changes/0070-social.xml`. PRD §3.1, §5.8
 */
package world.danchuo.social
