/**
 * Feature-слайс **theme** (PRD §3.1, §5.9; DESIGN §10) — пустой шов M0.
 *
 * Зона ответственности (M4): `Theme` — волны (именованные визуальные стили),
 * токены в JSONB; `GET /api/theme/active` отдаёт токены активной волны.
 * Новая волна = новая запись; смена активной меняет весь сайт без правок
 * компонентов; волна может переопределять layout-спаны плиток.
 */
package world.danchuo.theme
