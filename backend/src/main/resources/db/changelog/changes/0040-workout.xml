<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      The health slice (PRD §5.4, §7): workouts, N per day by date (a flat link,
      no FK relation to day_record: the slices are decoupled; the shared axis is the MSK date).
      Ingest idempotency is kept by replacing the set for a date → an index on date.
    -->
    <changeSet id="0040-workout-table" author="danchuo">
        <createTable tableName="workout">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="date" type="DATE">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="duration_minutes" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="active_energy_kcal" type="INT"/>
            <column name="distance_meters" type="INT"/>
            <column name="source" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex tableName="workout" indexName="idx_workout_date">
            <column name="date"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>
