<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      The spotify slice (PRD §M3): a singleton row with OAuth credentials. The refresh token is stored
      ENCRYPTED (AES-GCM, §8): the DB holds Base64(IV‖ciphertext), not plain text.
      One owner account ⇒ one row with a fixed id=1 (upsert on re-authorisation).
    -->
    <changeSet id="0050-spotify-token-table" author="danchuo">
        <createTable tableName="spotify_token">
            <column name="id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="encrypted_refresh_token" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="scope" type="VARCHAR(256)">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>
</databaseChangeLog>
