<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 01's edge, background and shadow (DESIGN §2.1, §2.3, §2.4). Tokens live in the DB and are injected into
      :root over the fallback frontend/src/app/styles/waves/wave-01.css, so a CSS edit alone
      is not enough, otherwise prod takes the old values from the seed and drifts from the code.

      An UPDATE over the applied 0080-theme-seed-wave01 rather than an edit of the seed itself: changeset
      0080 is already checksummed in prod, its checksum must not be touched (the same rule by which
      the text-tertiary AA fix once shipped, see the note in 0080). `||` merges only these keys,
      the rest of wave 01's token set is untouched.

      What changes (the wave 01 edge reference, DESIGN §2.4):
        bg-page       #faf1eb → #fdefe7: the peach is lighter and warmer; the background dot grid is removed
                      in CSS (not a token), the air between tiles is now silent under the pixel edge.
        border-tile   NEW: the colour of the thin clay line outlining the tile's stepped silhouette
                      entirely. The edge used to be drawn by the strong ink border border-pixel; that one
                      stays and holds the SELECTION ring (a calendar/week strip day,
                      the active wave swatch): it is a different outline.
        tile-line     NEW: the edge line's thickness.
        pixel-corners 2 steps of 4px (an 8px cut) → 2 steps of 5px (a 10px cut): the staircase is
                      larger and "straighter".
        drop-2/3/4    a neutral dark shadow straight down → a warm brown one offset
                      right-down: the peach tile's right edge is "lifted" off the background by a warm
                      shadow rather than a grey halo below. NB: the shadow is now actually visible: the edge
                      stopped cutting it with clip-path (the clip moved to the pseudo-layers, the shadow
                      stayed on the unclipped .pixel-tile; a latent bug of the old frame).

      Wave 02 "Obscura" is deliberately untouched: its cards draw their edge themselves (border+radius),
      it does not need these tokens (see the note "How a wave chooses its edge" in wave-01.css).
    -->
    <changeSet id="0160-wave-01-edge-tokens" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET tokens = tokens || $J${
  "bg-page": "#fdefe7",
  "border-tile": "#cc9f8c",
  "tile-line": "1.5px",
  "pixel-corners": "polygon(0 10px, 5px 10px, 5px 5px, 10px 5px, 10px 0, calc(100% - 10px) 0, calc(100% - 10px) 5px, calc(100% - 5px) 5px, calc(100% - 5px) 10px, 100% 10px, 100% calc(100% - 10px), calc(100% - 5px) calc(100% - 10px), calc(100% - 5px) calc(100% - 5px), calc(100% - 10px) calc(100% - 5px), calc(100% - 10px) 100%, 10px 100%, 10px calc(100% - 5px), 5px calc(100% - 5px), 5px calc(100% - 10px), 0 calc(100% - 10px))",
  "drop-2": "drop-shadow(2px 2px 1px rgba(83, 49, 27, 0.1)) drop-shadow(5px 7px 10px rgba(83, 49, 27, 0.2))",
  "drop-3": "drop-shadow(2px 3px 1px rgba(83, 49, 27, 0.12)) drop-shadow(8px 12px 16px rgba(83, 49, 27, 0.24))",
  "drop-4": "drop-shadow(3px 4px 2px rgba(83, 49, 27, 0.14)) drop-shadow(12px 17px 24px rgba(83, 49, 27, 0.26))"
}$J$::jsonb
WHERE key = 'wave-01';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET tokens = (tokens - 'border-tile' - 'tile-line') || $J${
  "bg-page": "#faf1eb",
  "pixel-corners": "polygon(0 8px, 4px 8px, 4px 4px, 8px 4px, 8px 0, calc(100% - 8px) 0, calc(100% - 8px) 4px, calc(100% - 4px) 4px, calc(100% - 4px) 8px, 100% 8px, 100% calc(100% - 8px), calc(100% - 4px) calc(100% - 8px), calc(100% - 4px) calc(100% - 4px), calc(100% - 8px) calc(100% - 4px), calc(100% - 8px) 100%, 8px 100%, 8px calc(100% - 4px), 4px calc(100% - 4px), 4px calc(100% - 8px), 0 calc(100% - 8px))",
  "drop-2": "drop-shadow(0 1px 1px rgba(33, 26, 22, 0.06)) drop-shadow(0 7px 11px rgba(33, 26, 22, 0.1))",
  "drop-3": "drop-shadow(0 2px 1px rgba(33, 26, 22, 0.08)) drop-shadow(0 12px 18px rgba(33, 26, 22, 0.14))",
  "drop-4": "drop-shadow(0 3px 2px rgba(33, 26, 22, 0.1)) drop-shadow(0 18px 26px rgba(33, 26, 22, 0.18))"
}$J$::jsonb
WHERE key = 'wave-01';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
