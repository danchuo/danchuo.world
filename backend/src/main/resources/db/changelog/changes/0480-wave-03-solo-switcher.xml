<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03 "PRIME": ONE wave switcher stays on the board (DESIGN §10.2).

      This is a working state, not the wave's final look: the canvas with the ribbon is tuned on an empty
      screen, and the widgets come back one at a time as each is dressed for glass. The earlier waves'
      layout is untouched: the hiding lives in this wave's layout delta.

      The switcher takes the centre of the 40x28 grid (it was pressed to the left edge next to the "Today"
      tile: without neighbours that corner read as an offcut). The switcher itself cannot be
      hidden: [UNHIDEABLE] in the frontend keeps it visible, otherwise a visitor would lock themselves
      into a wave with no way out.

      A caveat for the future: the delta lists tiles BY NAME, because that is all the mechanism can
      do. A tile added to the code later will not be in the list and will arrive on the board
      visible: a deliberate price for the merge's forward-compat (§10.1), not an oversight.
    -->
    <changeSet id="0480-wave-03-solo-switcher" author="danchuo">
        <sql><![CDATA[
UPDATE theme SET layout = $J${
  "tiles": {
    "waveSwitcher": { "col": 19, "row": 13, "colSpan": 4, "rowSpan": 4 },
    "identity":   { "hidden": true },
    "latestDrop": { "hidden": true },
    "freshness":  { "hidden": true },
    "music":      { "hidden": true },
    "photoDrops": { "hidden": true },
    "stats":      { "hidden": true },
    "sleep":      { "hidden": true },
    "today":      { "hidden": true },
    "calendar":   { "hidden": true },
    "projects":   { "hidden": true },
    "ride":       { "hidden": true },
    "hero":       { "hidden": true },
    "social":     { "hidden": true },
    "marquee":    { "hidden": true }
  },
  "mobileOrder": ["waveSwitcher"]
}$J$::jsonb WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme SET layout = NULL WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
