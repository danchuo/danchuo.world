<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03 dresses projects in 3D planets (DESIGN §10.2, §12.5): those with a model get
      a 3D artifact, the rest keep the flat sprite. Other waves carry no key and wear the planet
      flat: a cyan wireframe is foreign to wave 01's warm pixel art.

      The layout delta is rewritten WHOLE, as in 0660: `jsonb_set` on a deep path silently
      does nothing if an intermediate key is missing, and the whole `tiles.projects` branch is ours.
    -->
    <changeSet id="0680-wave-03-projects-planet" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,projects}',
  $J${"col": 5, "row": 23, "colSpan": 11, "rowSpan": 6, "edition": "console", "planet": "model"}$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,projects}',
  $J${"col": 5, "row": 23, "colSpan": 11, "rowSpan": 6, "edition": "console"}$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
