<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: Velobike returns to the board in the `map` edition (a map filling the tile, the latest
      ride's data as a blur band on top). The slot is under the drop ribbon, to the right of the latest
      drop tile: columns 17–25, rows 14–22.

      In the mobile stack the widget follows the drop ribbon: the same order as in bento
      (the ribbon on top, the map below it).

      The layout delta is rewritten WHOLE (as in 0660/0680/0700): `jsonb_set` on a deep
      path silently does nothing if an intermediate key is missing.
    -->
    <changeSet id="0710-wave-03-ride" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    layout,
    '{tiles,ride}',
    $J${"col": 17, "row": 14, "colSpan": 9, "rowSpan": 9, "edition": "map"}$J$::jsonb
  ),
  '{mobileOrder}',
  $J$["music", "latestDrop", "photoDrops", "ride", "projects", "waveSwitcher"]$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    layout,
    '{tiles,ride}',
    $J${"hidden": true}$J$::jsonb
  ),
  '{mobileOrder}',
  $J$["music", "latestDrop", "photoDrops", "projects", "waveSwitcher"]$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
