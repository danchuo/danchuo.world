<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: social moves to the top right corner, music stands right under it. The board's right
      column reads top to bottom as one column (links as a header, the player under them), and neither
      of the two tiles breaks off before the canvas edge: the space on the right is empty anyway.

      The order of marks in the row is instagram → telegram → x → github. Link sorting lives
      in the DATA (`social_link.sort_order`), not in the layout, so the reordering happens here.

      What stands where and which places were tried and rejected: DESIGN §10.2.
    -->
    <changeSet id="0760-wave-03-social-place" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,social}',
  $J${"col": 34, "row": 2, "colSpan": 7, "rowSpan": 2}$J$::jsonb
)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,music}',
  $J${"col": 31, "row": 5, "colSpan": 10, "rowSpan": 6}$J$::jsonb
)
WHERE key = 'wave-03';

UPDATE social_link SET sort_order = 40 WHERE platform = 'github';
UPDATE social_link SET sort_order = 10 WHERE platform = 'instagram';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(layout, '{tiles,social}', $J${"col": 16, "row": 24, "colSpan": 9, "rowSpan": 3}$J$::jsonb)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(layout, '{tiles,music}', $J${"col": 27, "row": 8, "colSpan": 10, "rowSpan": 6}$J$::jsonb)
WHERE key = 'wave-03';

UPDATE social_link SET sort_order = 10 WHERE platform = 'github';
UPDATE social_link SET sort_order = 40 WHERE platform = 'instagram';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
