<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Волна 03: соцсети уходят в правый верхний угол, музыка встаёт прямо под ними. Правая
      колонна борда читается сверху вниз одним столбцом — ссылки шапкой, плеер под ними, — и ни
      одна из двух плиток не обрывается раньше кромки холста: место справа всё равно пустует.

      Порядок знаков в ряду — instagram → telegram → x → github. Сортировка ссылок живёт
      в ДАННЫХ (`social_link.sort_order`), а не в вёрстке, поэтому перестановка — здесь.

      Что где стоит и какие места пройдены и отклонены — DESIGN §10.2.
    -->
    <changeSet id="0760-wave-03-social-place" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,social}',
  $J${"col": 34, "row": 2, "colSpan": 7, "rowSpan": 2}$J$::jsonb
)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,music}',
  $J${"col": 31, "row": 5, "colSpan": 10, "rowSpan": 6}$J$::jsonb
)
WHERE key = 'wave-03';

UPDATE social_link SET sort_order = 40 WHERE platform = 'github';
UPDATE social_link SET sort_order = 10 WHERE platform = 'instagram';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(layout, '{tiles,social}', $J${"col": 16, "row": 24, "colSpan": 9, "rowSpan": 3}$J$::jsonb)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(layout, '{tiles,music}', $J${"col": 27, "row": 8, "colSpan": 10, "rowSpan": 6}$J$::jsonb)
WHERE key = 'wave-03';

UPDATE social_link SET sort_order = 10 WHERE platform = 'github';
UPDATE social_link SET sort_order = 40 WHERE platform = 'instagram';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
