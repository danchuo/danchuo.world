<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03, the right column: social shrinks to ONE row, music moves up into
      the freed space, the wave switcher loses a row at the top.

      The social marks are sized from their tile's WIDTH (the `social-icon-size` token, in cqw), not from its
      height, so the second row gave the marks nothing: it was air under them.
      Removing it, we keep the old gap to music: social ended on row 3 with music from
      row 5, i.e. a one-row gap; now social ends on row 2, and music starts at
      row 4. It is music that has to move: otherwise the gap would double and the column would fall apart into
      two unrelated groups.

      The switcher shrinks from the top: its bottom (row 19) keeps it aligned with the Velobike tile
      under it, and the extra row was above its content.
    -->
    <changeSet id="0790-wave-03-right-column" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,social}',
      $J${"col": 34, "row": 2, "colSpan": 7, "rowSpan": 1, "edition": "peek"}$J$::jsonb
    ),
    '{tiles,music}',
    $J${"col": 31, "row": 4, "colSpan": 10, "rowSpan": 6}$J$::jsonb
  ),
  '{tiles,waveSwitcher}',
  $J${"col": 29, "row": 17, "colSpan": 6, "rowSpan": 3}$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,social}',
      $J${"col": 34, "row": 2, "colSpan": 7, "rowSpan": 2, "edition": "peek"}$J$::jsonb
    ),
    '{tiles,music}',
    $J${"col": 31, "row": 5, "colSpan": 10, "rowSpan": 6}$J$::jsonb
  ),
  '{tiles,waveSwitcher}',
  $J${"col": 29, "row": 16, "colSpan": 6, "rowSpan": 4}$J$::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
