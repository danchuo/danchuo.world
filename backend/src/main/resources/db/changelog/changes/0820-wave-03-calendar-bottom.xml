<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the calendar moves to the very bottom of the board, the wave switcher goes up top.

      The calendar takes the same 9 by 10 cells, but its bottom edge lands on the grid's last row
      (19..28). The switcher sits in the top left (columns 5..10, rows 2..4) above
      the drop ribbon, along its left edge: the top right is taken by social.

      The positions are edited by a MERGE (jsonb ||), not by replacing the whole record: both tiles
      also have sizes and an edition in the delta, and a replacement would erase them.

      The mobile order follows the board: the switcher first, the calendar last.
    -->
    <changeSet id="0820-wave-03-calendar-bottom" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,calendar}',
      (layout->'tiles'->'calendar') || '{"col":19,"row":19}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":5,"row":2}'::jsonb
  ),
  '{mobileOrder}',
  '["waveSwitcher","music","latestDrop","photoDrops","ride","projects","social","calendar"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,calendar}',
      (layout->'tiles'->'calendar') || '{"col":19,"row":13}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":29,"row":17}'::jsonb
  ),
  '{mobileOrder}',
  '["music","latestDrop","calendar","photoDrops","ride","projects","waveSwitcher","social"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
