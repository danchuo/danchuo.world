<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Волна 03: правый нижний угол занимает велобайк, над ним встаёт переключатель волн,
      оба прижаты к правому краю сетки (колонка 40).

      Велобайк — колонки 34..40, строки 22..28; переключатель — колонки 35..40, строки 18..20.
      Между ними остаётся строка воздуха, чтобы плитки не читались одной колонкой.

      Календарь теряет строку по вертикали: его сетка показывает три недели вместо четырёх
      (будущая неделя в неё больше не попадает, DESIGN 5.2), и десятая строка плитке стала
      не нужна. Нижний край остаётся на последней строке борда, поэтому сдвигается верхний.
    -->
    <changeSet id="0830-wave-03-right-column" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,ride}',
      (layout->'tiles'->'ride') || '{"col":34,"row":22}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":35,"row":18}'::jsonb
  ),
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":20,"rowSpan":9}'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,ride}',
      (layout->'tiles'->'ride') || '{"col":29,"row":21}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":5,"row":2}'::jsonb
  ),
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":19,"rowSpan":10}'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
