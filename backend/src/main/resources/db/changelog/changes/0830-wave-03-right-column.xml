<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: Velobike takes the bottom right corner, the wave switcher stands above it,
      both pressed to the grid's right edge (column 40).

      Velobike: columns 34..40, rows 22..28; the switcher: columns 35..40, rows 18..20.
      A row of air remains between them so the tiles do not read as one column.

      The calendar loses a row vertically: its grid shows three weeks instead of four
      (the future week no longer falls into it, DESIGN 5.2), and the tile no longer needs
      a tenth row. The bottom edge stays on the board's last row, so the top one moves.
    -->
    <changeSet id="0830-wave-03-right-column" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,ride}',
      (layout->'tiles'->'ride') || '{"col":34,"row":22}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":35,"row":18}'::jsonb
  ),
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":20,"rowSpan":9}'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    jsonb_set(
      layout,
      '{tiles,ride}',
      (layout->'tiles'->'ride') || '{"col":29,"row":21}'::jsonb
    ),
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col":5,"row":2}'::jsonb
  ),
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":19,"rowSpan":10}'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
