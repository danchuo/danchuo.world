<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the calendar loses one more row at the top: columns 19..27, rows 21..28.

      The "field" grid holds three rows at any tile height (DESIGN 5.2), so the extra
      height went not to the cells but to the air between them. The bottom edge stays on the board's last
      row: the "today" pill sits on it, and there is nowhere to move it.
    -->
    <changeSet id="0840-wave-03-calendar-shorter" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":21,"rowSpan":8}'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,calendar}',
  (layout->'tiles'->'calendar') || '{"row":20,"rowSpan":9}'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
