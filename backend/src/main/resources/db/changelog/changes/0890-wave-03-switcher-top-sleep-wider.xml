<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the wave switcher moves up into the left column's header, the "echo sounder" takes
      the freed row (DESIGN §10.2).

      The switcher: columns 5..10, rows 2..4: above the latest drop tile and along its left
      edge. Row 5 stays an empty track, i.e. the air between them is the same as between all
      neighbours on the board.

      Because of this the sounding gets a row further down (11..17 instead of 11..16) and gives up a track on the left
      (32..40 instead of 31..40): the board's right edge holds its right edge, and the left one aligns
      with the music tile one track narrower. There are no more rows under the switcher, and nothing
      to run into now.
    -->
    <changeSet id="0890-wave-03-switcher-top-sleep-wider" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    layout,
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col": 5, "row": 2, "colSpan": 6, "rowSpan": 3}'::jsonb
  ),
  '{tiles,sleep}',
  (layout->'tiles'->'sleep') || '{"col": 32, "row": 11, "colSpan": 9, "rowSpan": 7}'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  jsonb_set(
    layout,
    '{tiles,waveSwitcher}',
    (layout->'tiles'->'waveSwitcher') || '{"col": 35, "row": 18, "colSpan": 6, "rowSpan": 3}'::jsonb
  ),
  '{tiles,sleep}',
  (layout->'tiles'->'sleep') || '{"col": 31, "row": 11, "colSpan": 10, "rowSpan": 6}'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
