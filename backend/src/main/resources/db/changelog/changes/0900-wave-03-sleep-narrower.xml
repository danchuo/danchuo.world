<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the "echo sounder" gives up one more track on the left: columns 33..40 instead of 32..40 (DESIGN §10.2).

      The right edge holds the board's edge, like the music tile above it, and the left one moves right:
      the sounding becomes two tracks narrower than music, and the wave's ribbon opens between them. The height
      and rows do not change: only the width narrows.
    -->
    <changeSet id="0900-wave-03-sleep-narrower" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,sleep}',
  (layout->'tiles'->'sleep') || '{"col": 33, "row": 11, "colSpan": 8, "rowSpan": 7}'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,sleep}',
  (layout->'tiles'->'sleep') || '{"col": 32, "row": 11, "colSpan": 9, "rowSpan": 7}'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
