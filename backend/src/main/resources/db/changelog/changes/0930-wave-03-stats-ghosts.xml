<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the stats tile returns to the board in the "ghosts" edition (DESIGN §7.4, §10.2).

      Until now it was hidden on this wave: the default layout (two bars with a column of
      readings on the left) is wave 01's language, and on glass it read as a guest. "Ghosts" draws all
      metrics at once, each on its own scale, lighting the selected one: the answer usually lies in a pair, and a single
      curve cannot say "steps dropped the week sleep grew".

      Place: the middle of the board, columns 19..27, rows 12..19 (335×262 at the 1536 reference width).
      Above, the drop carousel ends at row 10, below, the calendar starts at 21, so
      rows 11 and 20 stay empty tracks: the air is as structural as for the other
      neighbours. Columns 28..30 stay an inter-column gap through which the canvas shows.
    -->
    <changeSet id="0930-wave-03-stats-ghosts" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,stats}',
  (layout->'tiles'->'stats') || '{"hidden": false, "edition": "ghosts", "col": 19, "row": 12, "colSpan": 9, "rowSpan": 8}'::jsonb
)
WHERE key = 'wave-03';

-- В мобильном стеке плитка встаёт сразу за музыкой: там же, где на борде она держит середину,
-- и до лент дропов, которые в стеке занимают больше всего высоты.
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{mobileOrder}',
  '["waveSwitcher", "music", "stats", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(layout, '{tiles,stats}', '{"hidden": true}'::jsonb)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(
  layout,
  '{mobileOrder}',
  '["waveSwitcher", "music", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
