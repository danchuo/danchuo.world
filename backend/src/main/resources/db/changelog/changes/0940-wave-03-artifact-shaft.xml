<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the artifacts tile returns to the board in the "shaft" edition (DESIGN §7.2, §10.2).

      Until now it was hidden: the default marquee of flat PNGs is wave 01's language, and on
      glass it read as a guest. The "shaft" stands the artifacts as 3D items in a line receding
      from the viewer: the far ones are smaller by honest perspective and sink into the same blur the tile
      uses on the canvas: the wave's material works as a rangefinder, and no separate scale is needed.

      Place: columns 12..18, rows 22..28 (263×200 at the 1536 reference width). No neighbours had to
      move: this is an already empty corner between the projects (which end at column 11) and the calendar
      (which starts at 19), and above, the "latest drop" ends at row 21. The hole in the layout
      closes, and no tile moved or changed size.

      In the mobile stack the tile goes last: it is a collection, not an event of the day, and scrolling
      to it past today's readings is the right order, not a loss.
    -->
    <changeSet id="0940-wave-03-artifact-shaft" author="danchuo">
        <sql><![CDATA[
UPDATE theme
SET layout = jsonb_set(
  layout,
  '{tiles,marquee}',
  (layout->'tiles'->'marquee') || '{"hidden": false, "edition": "shaft", "col": 12, "row": 22, "colSpan": 7, "rowSpan": 7}'::jsonb
)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(
  layout,
  '{mobileOrder}',
  '["waveSwitcher", "music", "stats", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar", "marquee"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme
SET layout = jsonb_set(layout, '{tiles,marquee}', '{"hidden": true}'::jsonb)
WHERE key = 'wave-03';

UPDATE theme
SET layout = jsonb_set(
  layout,
  '{mobileOrder}',
  '["waveSwitcher", "music", "stats", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar"]'::jsonb
)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
