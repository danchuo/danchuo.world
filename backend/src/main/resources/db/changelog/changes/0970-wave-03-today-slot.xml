<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: "Today" returns to the board (PRD §9.8, I-65). The tile arrives in the default
      edition: its place is cleared now, the tile's look is tuned separately.

      No tile's size is touched: the room is gained by shifting. The right column (social,
      music, sleep, switcher, Velobike) does not move at all: it stands tight already,
      rows 2..28, Velobike pinned to the bottom edge. The middle column stays in place, only
      the drop ribbon moves up to it (19 → 21), joining one column with stats and the calendar.

      The left half frees the room: the "latest drop" moves into the very corner (5,6 → 1,1), and
      the artifacts and projects gather into the channel between it and the board's middle (columns 13..19).
      Under them a band 1..19 × 18..28 opens: that is where "Today" stands.

      The board's density grows from 59.6% to 78.2%, and "Today" is again the wave's largest tile
      (19×11 against the drop's 11×16: 714×327 against 414×475 pixels at the owner's reference).

      No collisions, an empty track between neighbours is kept everywhere: the drop ends at row 16,
      the projects at row 14, "Today" starts at row 18; on the right the channel stops at column
      19, the middle column starts at 21.
    -->
    <changeSet id="0970-wave-03-today-slot" author="danchuo">
        <sql><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,latestDrop}',
  layout->'tiles'->'latestDrop' || '{"col": 1, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,marquee}',
  layout->'tiles'->'marquee' || '{"col": 13, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,projects}',
  layout->'tiles'->'projects' || '{"col": 13, "row": 9}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops}',
  layout->'tiles'->'photoDrops' || '{"col": 21}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,today}',
  '{"col": 1, "row": 18, "colSpan": 19, "rowSpan": 11, "hidden": false}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{mobileOrder}',
  '["today", "waveSwitcher", "music", "stats", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar", "marquee"]'::jsonb)
  WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,latestDrop}',
  layout->'tiles'->'latestDrop' || '{"col": 5, "row": 6}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,marquee}',
  layout->'tiles'->'marquee' || '{"col": 13, "row": 22}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,projects}',
  layout->'tiles'->'projects' || '{"col": 5, "row": 23}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops}',
  layout->'tiles'->'photoDrops' || '{"col": 19}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,today}', '{"hidden": true}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{mobileOrder}',
  '["waveSwitcher", "music", "stats", "latestDrop", "photoDrops", "ride", "projects", "social", "calendar", "marquee"]'::jsonb)
  WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
