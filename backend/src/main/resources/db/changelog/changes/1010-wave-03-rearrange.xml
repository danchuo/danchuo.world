<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 03: the board is rebuilt by the owner's rules. No tile's size is touched,
      the set is untouched: these are pure shifts.

      Rules: the wave switcher and social do not stand next to each other; social in the upper part; Spotify
      top left; sleep top right; "Today" in the centre; the calendar under it; Velobike bottom
      right; the projects bottom left.

      Three columns. Left 1..11: the drop (1..16), the projects (18..23), the wave switcher (25..26).
      Middle 13..31: Spotify and the artifact shaft on top, "Today" exactly in the grid's centre
      (13..28 × 9..19, the middle column 20.5 with the board's centre at 20.5), the calendar and stats under it.
      Right 33..40: sleep, social, the drop ribbon, Velobike.

      A DEVIATION made deliberately: Spotify stands not in the leftmost column but at the top
      of the middle one. The reason is arithmetic, not taste. The calendar under "Today" pins the tile
      to rows 9..19, so 7 rows remain above it in the middle, and any tile 8+ tall
      must live in a side column. There are three: the drop (16), the drop ribbon (9), stats (8). The left
      column after Spotify gives 21 rows: only the drop fits there; the right one after sleep and
      Velobike gives 12: only one of the remaining two fits there. One tall tile is left with
      no place at all. So the drop takes the whole left column (the only place
      where 11x16 fits), and Spotify stands at the top of the middle: it stays on top and left of
      centre, and this is the cheapest possible deviation.

      An empty track between every pair of neighbours is kept, checked cell by cell.
    -->
    <changeSet id="1010-wave-03-rearrange" author="danchuo">
        <sql><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,latestDrop}',
  layout->'tiles'->'latestDrop' || '{"col": 1, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,projects}',
  layout->'tiles'->'projects' || '{"col": 1, "row": 18}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,waveSwitcher}',
  layout->'tiles'->'waveSwitcher' || '{"col": 1, "row": 25}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,music}',
  layout->'tiles'->'music' || '{"col": 13, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,marquee}',
  layout->'tiles'->'marquee' || '{"col": 24, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,today}',
  layout->'tiles'->'today' || '{"col": 13, "row": 9}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar}',
  layout->'tiles'->'calendar' || '{"col": 13, "row": 21}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,stats}',
  layout->'tiles'->'stats' || '{"col": 23, "row": 21}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,sleep}',
  layout->'tiles'->'sleep' || '{"col": 33, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,social}',
  layout->'tiles'->'social' || '{"col": 33, "row": 9}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops}',
  layout->'tiles'->'photoDrops' || '{"col": 33, "row": 11}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,ride}',
  layout->'tiles'->'ride' || '{"col": 33, "row": 21}'::jsonb) WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,latestDrop}',
  layout->'tiles'->'latestDrop' || '{"col": 1, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,projects}',
  layout->'tiles'->'projects' || '{"col": 1, "row": 18}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,waveSwitcher}',
  layout->'tiles'->'waveSwitcher' || '{"col": 1, "row": 25}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,music}',
  layout->'tiles'->'music' || '{"col": 31, "row": 20}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,marquee}',
  layout->'tiles'->'marquee' || '{"col": 23, "row": 21}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,today}',
  layout->'tiles'->'today' || '{"col": 13, "row": 9}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar}',
  layout->'tiles'->'calendar' || '{"col": 13, "row": 21}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,stats}',
  layout->'tiles'->'stats' || '{"col": 31, "row": 11}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,sleep}',
  layout->'tiles'->'sleep' || '{"col": 13, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,social}',
  layout->'tiles'->'social' || '{"col": 1, "row": 28}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops}',
  layout->'tiles'->'photoDrops' || '{"col": 31, "row": 1}'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,ride}',
  layout->'tiles'->'ride' || '{"col": 22, "row": 1}'::jsonb) WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
