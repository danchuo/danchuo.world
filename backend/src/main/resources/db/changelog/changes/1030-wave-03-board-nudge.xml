<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Волна 03: доводка борда по трекам. Размеры не тронуты ни у одной плитки, состав тот же.

      Лента дропов ещё на трек влево (32 → 31), переключатель волн на два трека вправо (1 → 3),
      календарь на трек влево (23 → 22).

      Переключатель, сойдя с левого края, встаёт кол. 3..8 и упирается в статы (9..17) вплотную по
      колонкам — но расходится с ними по рядам (18..19 против 21..28), так что пустой трек между
      соседями остаётся. Лента дропов 31..38 расходится с «Сегодня» (13..28) через треки 29..30 и
      с календарём (22..30) по рядам. Проверено поклеточно.
    -->
    <changeSet id="1030-wave-03-board-nudge" author="danchuo">
        <sql><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops,col}', '31'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,waveSwitcher,col}', '3'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar,col}', '22'::jsonb) WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops,col}', '32'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,waveSwitcher,col}', '1'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar,col}', '23'::jsonb) WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
