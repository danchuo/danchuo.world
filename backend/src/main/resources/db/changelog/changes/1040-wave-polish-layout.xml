<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Wave 02: on the phone Spotify and the calendar swap places: the calendar comes right after
      "Today", music after it. Bento is untouched, this is only the stack order.

      Wave 03, bento. "Today" and the drop ribbon a track right (13 → 14 and 31 → 32), the charts and
      the calendar a track shorter at the bottom (rowSpan 8 → 7, the start row is the same).

      Cell by cell: "Today" stands at col. 14..29 and clears the drop ribbon (32..39) across the empty
      tracks 30..31; in rows (9..19 against 11..19) they would not meet anyway, but the eye needs the gap.
      The ribbon stops at col. 39 with a 40 grid: the last track stays a margin. The charts
      (9..17) and the calendar (22..30) free row 28, the board's bottom track becomes air.

      Wave 03, phone: the order is set in full: "Today", calendar, sleep, Spotify, charts,
      latest drop, Velobike, drops, social, projects, artifacts, waves. The wave switcher is
      last: it is a utility and does not get in the way at the bottom. Sleep was not listed in the old list at all and
      fell to the tail by default: now it stands third explicitly.
    -->
    <changeSet id="1040-wave-polish-layout" author="danchuo">
        <sql><![CDATA[
UPDATE theme SET layout = jsonb_set(
    layout, '{mobileOrder}',
    '["identity","today","calendar","music","stats","sleep","ride","latestDrop","photoDrops","projects","social","marquee","waveSwitcher","freshness"]'::jsonb)
WHERE key = 'wave-02';

UPDATE theme SET layout = jsonb_set(layout, '{tiles,today,col}', '14'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops,col}', '32'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,stats,rowSpan}', '7'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar,rowSpan}', '7'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(
    layout, '{mobileOrder}',
    '["today","calendar","sleep","music","stats","latestDrop","ride","photoDrops","social","projects","marquee","waveSwitcher"]'::jsonb)
WHERE key = 'wave-03';
        ]]></sql>
        <rollback><![CDATA[
UPDATE theme SET layout = jsonb_set(
    layout, '{mobileOrder}',
    '["identity","today","music","calendar","stats","sleep","ride","latestDrop","photoDrops","projects","social","marquee","waveSwitcher","freshness"]'::jsonb)
WHERE key = 'wave-02';

UPDATE theme SET layout = jsonb_set(layout, '{tiles,today,col}', '13'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,photoDrops,col}', '31'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,stats,rowSpan}', '8'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(layout, '{tiles,calendar,rowSpan}', '8'::jsonb) WHERE key = 'wave-03';
UPDATE theme SET layout = jsonb_set(
    layout, '{mobileOrder}',
    '["today","waveSwitcher","music","stats","latestDrop","photoDrops","ride","projects","social","calendar","marquee"]'::jsonb)
WHERE key = 'wave-03';
        ]]></rollback>
    </changeSet>
</databaseChangeLog>
