<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Тренировки сняты целиком: сущность, репозиторий, приём workouts[] в ingest и поле в DayView
      (PRD §5.4). Канала нет — шорткат их не шлёт, на борде они не показывались, таблица год
      простояла пустой. Тот же случай, что и колонка screen_time_minutes в 0730: guardrail §3.1
      запрещает нести мёртвый код в ожидании источника.

      Появится канал — сущность заводится вместе с ним, одной миграцией. Откат воссоздаёт таблицу.
    -->
    <changeSet id="1060-drop-workout" author="danchuo">
        <dropTable tableName="workout"/>
        <rollback>
            <createTable tableName="workout">
                <column name="id" type="BIGINT" autoIncrement="true">
                    <constraints primaryKey="true" nullable="false"/>
                </column>
                <column name="date" type="DATE">
                    <constraints nullable="false"/>
                </column>
                <column name="type" type="VARCHAR(64)">
                    <constraints nullable="false"/>
                </column>
                <column name="duration_minutes" type="INT">
                    <constraints nullable="false"/>
                </column>
                <column name="active_energy_kcal" type="INT"/>
                <column name="distance_meters" type="INT"/>
                <column name="source" type="VARCHAR(32)">
                    <constraints nullable="false"/>
                </column>
            </createTable>
            <createIndex tableName="workout" indexName="idx_workout_date">
                <column name="date"/>
            </createIndex>
        </rollback>
    </changeSet>
</databaseChangeLog>
