<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Workouts are removed entirely: the entity, the repository, the workouts[] ingest and the DayView field
      (PRD §5.4). There is no channel: the shortcut does not send them, they were never shown on the board, the table
      stood empty for a year. The same case as the screen_time_minutes column in 0730: guardrail §3.1
      forbids carrying dead code while waiting for a source.

      When a channel appears, the entity is created together with it, in one migration. The rollback recreates the table.
    -->
    <changeSet id="1060-drop-workout" author="danchuo">
        <dropTable tableName="workout"/>
        <rollback>
            <createTable tableName="workout">
                <column name="id" type="BIGINT" autoIncrement="true">
                    <constraints primaryKey="true" nullable="false"/>
                </column>
                <column name="date" type="DATE">
                    <constraints nullable="false"/>
                </column>
                <column name="type" type="VARCHAR(64)">
                    <constraints nullable="false"/>
                </column>
                <column name="duration_minutes" type="INT">
                    <constraints nullable="false"/>
                </column>
                <column name="active_energy_kcal" type="INT"/>
                <column name="distance_meters" type="INT"/>
                <column name="source" type="VARCHAR(32)">
                    <constraints nullable="false"/>
                </column>
            </createTable>
            <createIndex tableName="workout" indexName="idx_workout_date">
                <column name="date"/>
            </createIndex>
        </rollback>
    </changeSet>
</databaseChangeLog>
